[package]
name = "scipion-kernel"
version = "0.1.0"
edition = "2021"

[dependencies]
limine = "0.6.3" 

[profile.dev]
panic = "abort"

[profile.release]
panic = "abort"
codegen-units = 1
lto = true
opt-level = "z"